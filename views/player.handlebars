<main id="player-bg">
    <div class="container">
        {{#each playerData}}
        <div class="row">
            {{> character-button}}
        </div>
        {{/each}}

        {{!-- {{#each enemyData}}
        <div class="row">
            {{> enemy-button}}
        </div>
        {{/each}} --}}

        {{#each playerData}}
        <div class="row">
            {{> playerinfo}}
        </div>
        {{/each}}

        {{!-- {{#each enemyData}}
        <div class="row">
            {{> enemyinfo}}
        </div>
        {{/each}} --}}

    </div>

</main>