<main id="player-bg">
    <div class="container">
        {{#each playerData}}
        <div class="row">
            {{> character-button}}
        </div>
        {{/each}}

        {{#each playerData}}
        <div class="row">
            {{> character}}
        </div>
        {{/each}}
    </div>

</main>